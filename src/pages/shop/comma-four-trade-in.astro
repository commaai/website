---
import Base from '../../layouts/Base.astro';
import Layout from '../../routes/+layout.svelte';
import Page from '../../routes/shop/comma-four-trade-in/+page.svelte';
import { setupPage } from '$lib/shims/setup.js';

const { blogPosts } = await setupPage(Astro);
---

<Base title="comma four trade-in — comma shop">
  <Layout client:load {blogPosts}>
    <Page client:load />
  </Layout>
</Base>
