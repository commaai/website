{
  "name": "website",
  "author": "comma.ai <x@comma.ai>",
  "version": "0.2.0",
  "private": true,
  "scripts": {
    "dev": "hugo server",
    "build": "hugo --gc --cleanDestinationDir",
    "preview": "python3 scripts/serve.py 8080 build",
    "images": "bun scripts/optimize-images.mjs",
    "deploy": "bunx firebase deploy"
  },
  "devDependencies": {
    "sharp": "^0.34.2"
  },
  "type": "module"
}
